@model IEnumerable<WebApplication2.tDolzhnost>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naimenovanie)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naimenovanie)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DolzhnostID }) |
            @Html.ActionLink("Details", "Details", new { id=item.DolzhnostID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DolzhnostID })
        </td>
    </tr>
}

</table>
