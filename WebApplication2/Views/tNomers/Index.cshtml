@model IEnumerable<WebApplication2.tNomer>

@{
    ViewBag.Title = "Nomers";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Zanyat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tip_nomera)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Zanyat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tip_nomera)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Nomer }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Nomer }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Nomer })
        </td>
    </tr>
}

</table>
