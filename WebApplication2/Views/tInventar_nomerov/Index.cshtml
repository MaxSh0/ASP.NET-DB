@model IEnumerable<WebApplication2.tInventar_nomerov>

@{
    ViewBag.Title = "Inventar_nomerov";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Naimenovanie)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Kolvo_v_nomere)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tNomer.Tip_nomera)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Naimenovanie)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Kolvo_v_nomere)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tNomer.Tip_nomera)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_inventarnoy_edinitsy }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_inventarnoy_edinitsy }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_inventarnoy_edinitsy })
        </td>
    </tr>
}

</table>
